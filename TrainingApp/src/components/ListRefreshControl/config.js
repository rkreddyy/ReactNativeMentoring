import { RefreshControl } from 'react-native';

import { colors } from '../../assets/styles/colors';

export const appearanceProps = {
  colors: [colors.bostonBlue],
  size: RefreshControl?.SIZE?.LARGE
};
